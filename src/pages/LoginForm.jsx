import { Outlet } from "react-router-dom"

const LoginForm = () => {
    return (
        <div>
            <h1>Formulario</h1>

            <Outlet />
        </div>
    )
}

export default LoginForm
